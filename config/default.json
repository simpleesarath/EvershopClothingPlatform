{
  "shop": {
    "currency": "INR"
  },
  "system": {
    "database": {
      "host": "${DB_HOST}",
      "port": "${DB_PORT}",
      "database": "${DB_NAME}",
      "user": "${DB_USER}",
      "password": "${DB_PASSWORD}",
      "ssl": true
    }
  },
  "server": {
    "port": "process.env.PORT || 3000"
  }
}
